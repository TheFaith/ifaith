<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper 
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ifaith.fellowship.userMapper">
    <select id="getUser" parameterType="int" resultType="com.ifaith.fellowship.entity.user.UserBasicInfo">
        <![CDATA[
        SELECT 
			[AuthenticateConsumerID] AS SysNo,
			[ConsumerAppID] AS Age,
			[ConsumerName] AS Name
		FROM [dbo].[AuthenticateConsumer] WITH(NOLOCK)
		WHERE [AuthenticateConsumerID] = #{id}
		]]>
    </select>
</mapper>